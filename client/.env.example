# Public base URL for the deployed Render API (no trailing slash).
VITE_API_URL=http://localhost:5000

# Google reCAPTCHA v2 site key (checkbox). Leave empty to skip in local UI.
VITE_RECAPTCHA_SITE_KEY=
